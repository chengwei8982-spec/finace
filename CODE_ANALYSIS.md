# 代码分析（stock_agent_mvp.py）

## 总体定位
- 这是一个**单文件**的股票决策支持 MVP：输入市场/板块/个股特征，输出当日交易计划（JSON + Markdown）。
- 支持两种数据模式：
  - `mock`：内置样本数据，保证可运行。
  - `live`：通过 `yfinance` 下载日线数据。

## 架构分层
1. **配置层**：观察列表、板块映射、风控阈值。
2. **数据模型层**：`dataclass` 定义输入特征与输出对象。
3. **数据构建层**：mock 构建与 live 构建。
4. **策略 Agent 层**：
   - 市场环境识别（`MarketRegimeAgent`）
   - 板块轮动（`SectorRotationAgent`）
   - 个股打分（`StockScoringAgent`）
   - 风险控制（`RiskAgent`）
   - 复盘反思（`ReflectionAgent`）
5. **Pipeline 层**：串联各 Agent，过滤候选并形成 `DailyPlan`。
6. **报告层**：终端摘要 + JSON/Markdown 文件输出。

## 关键优点
- **可解释性强**：打分拆分为 `trend/flow/execution/sector`，并附 `rationale`。
- **容错设计**：live 失败自动回退 mock，避免任务中断。
- **风控前置**：统一输出禁止动作与仓位上限，便于执行层接入。
- **工程上手门槛低**：单文件便于快速迭代和演示。

## 主要问题与风险
1. **规则与阈值硬编码较多**
   - 多处阈值直接写在逻辑中，后续难以系统调参。
2. **板块映射静态化**
   - `infer_sector_for_ticker` 强依赖预设字典，新增标的泛化能力弱。
3. **无回测闭环**
   - 当前是“当日建议生成器”，缺少收益/回撤统计与参数验证。
4. **数据质量与异常处理较粗粒度**
   - live 下载失败多为 `continue`，可观测性一般（无细粒度日志）。
5. **仓位分配未落地**
   - 输出推荐列表但未提供“每只股票分配比例”的求解逻辑。

## 建议的演进路线（按优先级）
1. **参数外置化**：将阈值与权重迁移到 YAML/JSON 配置。
2. **结果评估模块**：新增轻量回测脚本（命中率、盈亏比、最大回撤）。
3. **风控增强**：加入波动率目标仓位、相关性约束、组合层止损。
4. **数据与日志**：增加下载失败原因记录、缺失字段检查与质量评分。
5. **可扩展性**：将 Agent 拆分为包结构，保留单文件入口用于 demo。

## 适用场景与边界
- 适用：策略原型验证、团队讨论、教学演示。
- 不适用：实盘自动化交易（缺少交易执行、风控闭环、监控告警与审计）。
